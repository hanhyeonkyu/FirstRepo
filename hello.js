import react from 'react';

class hello extends React.Component{
    render(){
        return(
            <div>
                Git init - commit(with comment) 
                 git remote add origin [url]
            - fetch - pull - push
                git pull test - Success!
            </div>
        )
    }
}
